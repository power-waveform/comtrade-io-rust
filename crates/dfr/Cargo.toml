[package]
name = "comtrade-dfr"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"
description = "COMTRADE DFR 格式解析（只读）"
license = "MIT"
authors = ["sanguine (张松贵) <82018238@qq.com>"]

[dependencies]
comtrade-cbase = { version = "0.1.0", path = "../cbase" }
comtrade-cfg = { version = "0.1.0", path = "../cfg" }
comtrade-dat = { version = "0.1.0", path = "../dat" }

[lib]
name = "dfr"
path = "src/lib.rs"